# Load the uinput kernel module at boot so the daemon can create virtual
# devices. Install to /etc/modules-load.d/:
#
#   sudo cp packaging/uinput.conf /etc/modules-load.d/uinput.conf
#   sudo modprobe uinput   # load now without rebooting
uinput
